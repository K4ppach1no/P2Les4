using System;
using System.Collections.Generic;
using System.Net;
using System.Text;

namespace P2Les4.Classes
{
    class Client
    {
        static string Socket(string message)
        {
            IPAddress ipAddress = IPAddress.Parse("192.168.137.1"); //Voer hier het IP adres van je computer in!
            IPEndPoint localEndPoint = new IPEndPoint(ipAddress, 11000); //Gebruik een andere poort als dat nodig is
            string 
            return sentMessage;
        }
    }
}
